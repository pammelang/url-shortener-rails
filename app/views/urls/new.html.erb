<h1>Input the URL you want to shorten!</h1>

<%= form_for :url, url: urls_path do |f| %>
  <%= f.text_field :original_url%>
  <%= f.submit 'Shorten!' %>

<% end %>

 <%= link_to 'Show me the details of my short url instead', urls_path %>
